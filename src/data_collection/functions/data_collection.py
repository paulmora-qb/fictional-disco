"""Functions for data collection."""

import pandas as pd


def load_prices(sp500_data: pd.DataFrame) -> pd.DataFrame:
    """Place holder.

    Args:
    ----
        sp500_data (pd.DataFrame): _description_

    Returns:
    -------
        pd.DataFrame: _description_

    """
    a = sp500_data
