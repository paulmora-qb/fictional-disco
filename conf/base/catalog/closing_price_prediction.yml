_csv: &csv
  type: "${_datasets.csv}"
  save_args:
    index: True
  load_args:
    index_col: Date

# Features #########################################################################

high_minus_low:
  <<: *csv
  filepath: ${_base_path}/${_folders.prm}/high_minus_low

close_minus_open:
  <<: *csv
  filepath: ${_base_path}/${_folders.prm}/close_minus_open

price_aggregation:
  <<: *csv
  filepath: ${_base_path}/${_folders.prm}/price_aggregation
