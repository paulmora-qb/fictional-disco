# Dataset ##############################################################################

sp500_stock_ticker:
  type: ${_datasets.pickle}
  filepath: ${_base_path}/${_folders.raw}/list_stock_symbols.pkl

price_data:
  type: ${_datasets.csv}
  filepath: ${_base_path}/${_folders.int}/price_data.csv
