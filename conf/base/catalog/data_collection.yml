_csv: &csv
  type: "${_datasets.csv}"
  save_args:
    index: True

# Dataset ##############################################################################

sp500_data:
  type: ${_datasets.csv}
  filepath: ${_base_path}/${_folders.raw}/sp500.csv

price_data:
  type: ${_datasets.partitioned}
  path: ${_base_path}/${_folders.int}/
  dataset:
    type: "${_datasets.csv}"
    load_args:
      index: True
    save_args:
      index: true
